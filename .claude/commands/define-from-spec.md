---
description: Author a SKILL.md from a normalized spec in specs/.
---

Invoke the `skill-author` agent on $ARGUMENTS (a spec filename in `specs/`).

If empty, list `specs/` and ask which one.

After the SKILL.md is staged, offer to run `/install-skill` with the appropriate target.
